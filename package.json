{
	"name": "obsidian-meeting-copilot",
	"version": "0.9.5",
	"description": "Record, transcribe, and summarize meetings on macOS with Google Calendar sync, dual-channel audio capture, on-device or remote transcription, and AI notes.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"build:swift": "cd swift-helper && swift build -c release",
		"build:all": "npm run build:swift && npm run build",
		"deploy:local": "node deploy-local.mjs",
		"version": "TAG=$npm_package_version node scripts/sync-release-version.mjs && git add manifest.json versions.json",
		"lint": "eslint .",
		"test": "vitest run"
	},
	"keywords": [],
	"license": "0-BSD",
	"devDependencies": {
		"@eslint/js": "9.30.1",
		"@eslint/json": "0.14.0",
		"@types/node": "^22.19.19",
		"esbuild": "0.25.5",
		"eslint-plugin-obsidianmd": "^0.4.2",
		"globals": "14.0.0",
		"jiti": "2.6.1",
		"tslib": "2.4.0",
		"typescript": "^5.8.3",
		"typescript-eslint": "8.35.1",
		"vitest": "^4.1.7"
	},
	"dependencies": {
		"@echogarden/fvad-wasm": "^0.2.0",
		"obsidian": "latest"
	}
}
